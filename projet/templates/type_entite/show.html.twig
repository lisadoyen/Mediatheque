<!DOCTYPE html>

<title>TypeEntite</title>

{% block body %}
    <h1>TypeEntite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_entite.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_entite.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_entite_index') }}">back to list</a>

    <a href="{{ path('type_entite_edit', {'id': type_entite.id}) }}">edit</a>

    {{ include('type_entite/_delete_form.html.twig') }}
{% endblock %}
