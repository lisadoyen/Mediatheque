<!DOCTYPE html>

<title>Lien index</title>

{% block body %}
    <h1>Lien index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Adresse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lien in liens %}
            <tr>
                <td>{{ lien.id }}</td>
                <td>{{ lien.adresse }}</td>
                <td>
                    <a href="{{ path('lien_show', {'id': lien.id}) }}">show</a>
                    <a href="{{ path('lien_edit', {'id': lien.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('lien_new') }}">Create new</a>
{% endblock %}
