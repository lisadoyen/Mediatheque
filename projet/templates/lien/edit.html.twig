<!DOCTYPE html>

<title>Edit Lien</title>

{% block body %}
    <h1>Edit Lien</h1>

    {{ include('lien/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lien_index') }}">back to list</a>

    {{ include('lien/_delete_form.html.twig') }}
{% endblock %}
