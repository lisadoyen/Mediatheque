<!DOCTYPE html>

<title>New Rubrique</title>

{% block body %}
    <h1>Create new Rubrique</h1>

    {{ include('rubrique/_form.html.twig') }}

    <a href="{{ path('rubrique_index') }}">back to list</a>
{% endblock %}
