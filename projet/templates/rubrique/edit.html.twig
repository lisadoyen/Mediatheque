<!DOCTYPE html>

<title>Edit Rubrique</title>

{% block body %}
    <h1>Edit Rubrique</h1>

    {{ include('rubrique/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('rubrique_index') }}">back to list</a>

    {{ include('rubrique/_delete_form.html.twig') }}
{% endblock %}
