<!DOCTYPE html>

<title>TrancheAge index</title>

{% block body %}
    <h1>TrancheAge index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>AgeMin</th>
                <th>AgeMax</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tranche_age in tranche_ages %}
            <tr>
                <td>{{ tranche_age.id }}</td>
                <td>{{ tranche_age.libelle }}</td>
                <td>{{ tranche_age.ageMin }}</td>
                <td>{{ tranche_age.ageMax }}</td>
                <td>
                    <a href="{{ path('tranche_age_show', {'id': tranche_age.id}) }}">show</a>
                    <a href="{{ path('tranche_age_edit', {'id': tranche_age.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tranche_age_new') }}">Create new</a>
{% endblock %}
